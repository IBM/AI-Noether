ArchiveEntry "escape_velocity_target1_combo1"

/* AI-Noether: KeyMaera Reasoning Template (Noisy Case)
 * 
 * This template verifies that the abducted axioms (with existentially quantified
 * coefficients) can derive the noisy consequence (also with existentially 
 * quantified coefficients).
 *
 * Structure:
 * - Variables are universally quantified (forall)
 * - Monomial coefficients are existentially quantified (exists)
 * - Known axioms appear as exact equations
 * - Abducted axioms have coefficients c1, c2, ... for each monomial
 * - Consequence has coefficients d1, d2, ... for each monomial
 */

ProgramVariables
  Real Eki;
  Real Ekf;
  Real Ugi;
  Real Ugf;
  Real G;
  Real M;
  Real m;
  Real ve;
  Real r;
  Real c1_1;
  Real c1_2;
  Real c1_3;
  Real c1_4;
  Real c1_5;
  Real c1_6;
  Real c1_7;
  Real c1_8;
  Real c1_9;
  Real c1_10;
  Real d1;
  Real d2;
End.

Problem
  (
    \exists c1_1 \exists c1_2 \exists c1_3 \exists c1_4 \exists c1_5 \exists c1_6 \exists c1_7 \exists c1_8 \exists c1_9 \exists c1_10 \exists d1 \exists d2
    (
      c1_1 > 0 & c1_2 > 0 & c1_3 > 0 & c1_4 > 0 & c1_5 > 0 & c1_6 > 0 & c1_7 > 0 & c1_8 > 0 & c1_9 > 0 & c1_10 > 0 & d1 > 0 & d2 > 0
      &
      (
        \forall Eki \forall Ekf \forall Ugi \forall Ugf \forall G \forall M \forall m \forall ve \forall r
        (
          (
            (Eki > 0 & Ekf > 0 & Ugi > 0 & Ugf > 0 & G > 0 & M > 0 & m > 0 & ve > 0 & r > 0) &
            & ( Ekf - 0 = 0 )
            & ( Ugi * r + G * M * m = 0 )
            & ( Ugf - 0 = 0 )
            & ( Eki + Ugi - Ekf - Ugf = 0 )
            & ( ( c1_1 * Eki ) + ( c1_2 * m*ve^2 ) = 0 )
            & ( ( c1_3 * Eki ) + ( c1_4 * m*ve^2 ) = 0 )
            & ( ( c1_5 * Eki ) + ( c1_6 * m*ve^2 ) = 0 )
            & ( ( c1_7 * Eki ) + ( c1_8 * m*ve^2 ) = 0 )
            & ( ( c1_9 * Eki ) + ( c1_10 * m*ve^2 ) = 0 )
          )
          ->
          (
            ( d1 * G*M*m ) + ( d2 * m*ve^2*r ) = 0
          )
        )
      )
    )
  )
End.

End.
